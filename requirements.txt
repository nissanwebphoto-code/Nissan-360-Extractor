:root {
  --nissan-red: #c3002f;
  --nissan-red-dark: #a00026;
  --nissan-black: #0a0a0a;
  --nissan-gray: #1a1a1a;
  --light-bg: #eceff1;
  --card-bg: #ffffff;
  --border-gray: #e3e3e3;
  --text-muted: #6b6b6b;
  --shadow-sm: 0 2px 12px rgba(0, 0, 0, 0.06);
  --shadow-md: 0 8px 30px rgba(0, 0, 0, 0.08);
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  font-family: 'Helvetica Neue', 'Segoe UI', Arial, sans-serif;
}

body {
  background: var(--light-bg);
  color: var(--nissan-black);
  min-height: 100vh;
  -webkit-font-smoothing: antialiased;
}

/* HEADER */
.header {
  background: linear-gradient(180deg, #111 0%, #000 100%);
  padding: 20px 48px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
  position: sticky;
  top: 0;
  z-index: 100;
}
.header-left { display: flex; align-items: center; gap: 22px; }
.logo { height: 44px; width: auto; }
.header-title {
  color: #fff; font-size: 14px; font-weight: 300; letter-spacing: 4px;
  text-transform: uppercase; border-left: 1px solid rgba(255,255,255,0.25); padding-left: 22px;
}
.header-title span { color: var(--nissan-red); font-weight: 700; }

/* HERO */
.main { max-width: 1000px; margin: 48px auto; padding: 0 24px; }
.hero { text-align: center; margin-bottom: 40px; }
.hero h1 { font-size: 38px; font-weight: 800; letter-spacing: -0.5px; margin-bottom: 14px; }
.hero h1 .accent { color: var(--nissan-red); }
.hero p { color: var(--text-muted); font-size: 16px; max-width: 640px; margin: 0 auto; line-height: 1.7; }

/* INPUT CARD */
.card {
  background: var(--card-bg); border-radius: 10px; padding: 36px;
  box-shadow: var(--shadow-md); border-top: 4px solid var(--nissan-red);
}
textarea {
  width: 100%; padding: 16px 18px; font-size: 14px;
  border: 2px solid var(--border-gray); border-radius: 8px; background: #fafafa;
  resize: vertical; font-family: 'SF Mono', 'Courier New', monospace; line-height: 1.6;
  transition: all 0.25s ease;
}
textarea:focus {
  outline: none; border-color: var(--nissan-red); background: #fff;
  box-shadow: 0 0 0 4px rgba(195, 0, 47, 0.08);
}
.input-actions {
  display: flex; align-items: center; justify-content: space-between;
  margin-top: 14px; gap: 14px;
}
.hint { font-size: 13px; color: var(--text-muted); font-weight: 600; }

.btn {
  background: var(--nissan-red); color: #fff; border: none; padding: 15px 36px;
  font-size: 14px; font-weight: 700; letter-spacing: 1.5px; text-transform: uppercase;
  border-radius: 8px; cursor: pointer; transition: all 0.25s ease; white-space: nowrap;
}
.btn:hover { background: var(--nissan-red-dark); transform: translateY(-1px); box-shadow: 0 6px 20px rgba(195,0,47,0.3); }
.btn:active { transform: translateY(0); }
.btn:disabled { background: #b0b0b0; cursor: not-allowed; transform: none; box-shadow: none; }

.btn-secondary {
  background: #fff; color: var(--nissan-black); border: 2px solid var(--border-gray);
  padding: 13px 24px; font-size: 13px; font-weight: 700; letter-spacing: 1px;
  text-transform: uppercase; border-radius: 8px; cursor: pointer; transition: all 0.2s;
  white-space: nowrap;
}
.btn-secondary:hover { border-color: var(--nissan-red); color: var(--nissan-red); }

/* STATUS */
.status { margin-top: 20px; font-size: 14px; color: var(--text-muted); min-height: 20px; }
.status.error { color: var(--nissan-red); font-weight: 600; }
.status.success { color: #2e7d32; font-weight: 600; }
.spinner {
  display: inline-block; width: 15px; height: 15px; border: 2px solid #ddd;
  border-top-color: var(--nissan-red); border-radius: 50%;
  animation: spin 0.8s linear infinite; margin-right: 9px; vertical-align: middle;
}
@keyframes spin { to { transform: rotate(360deg); } }

/* RESULTS */
.results { margin-top: 36px; }
.section {
  background: var(--card-bg); border-radius: 10px; margin-bottom: 22px;
  overflow: hidden; box-shadow: var(--shadow-sm); animation: fadeIn 0.4s ease;
}
@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

.section-header {
  background: linear-gradient(180deg, #222 0%, #141414 100%); color: #fff;
  padding: 18px 24px; display: flex; align-items: center; justify-content: space-between;
}
.section-header h3 {
  font-size: 16px; font-weight: 600; letter-spacing: 0.3px;
  display: flex; align-items: center; gap: 12px;
}
.badge {
  background: var(--nissan-red); color: #fff; font-size: 12px; padding: 3px 11px;
  border-radius: 20px; font-weight: 700; min-width: 26px; text-align: center;
}
.copy-all {
  background: rgba(255,255,255,0.12); color: #fff; border: 1px solid rgba(255,255,255,0.25);
  padding: 9px 18px; font-size: 12px; border-radius: 6px; cursor: pointer;
  font-weight: 600; transition: all 0.2s;
}
.copy-all:hover { background: var(--nissan-red); border-color: var(--nissan-red); }

.section-body { padding: 10px 24px 22px; }

.img-item {
  display: flex; align-items: center; justify-content: space-between;
  padding: 14px 0; border-bottom: 1px solid var(--border-gray); gap: 14px; transition: background 0.15s;
}
.img-item:hover { background: #fafafa; }
.img-item:last-child { border-bottom: none; }
.img-link {
  color: #0066cc; text-decoration: none; font-size: 12.5px; word-break: break-all;
  flex: 1; font-family: 'SF Mono', 'Courier New', monospace; line-height: 1.5;
}
.img-link:hover { text-decoration: underline; }
.btn-group { display: flex; gap: 8px; flex-shrink: 0; }
.mini-btn {
  background: var(--nissan-black); color: #fff; border: none; padding: 9px 18px;
  font-size: 12px; border-radius: 6px; cursor: pointer; font-weight: 600;
  transition: all 0.2s; white-space: nowrap;
}
.mini-btn:hover { background: var(--nissan-red); }
.mini-btn.dl { background: #2e7d32; }
.mini-btn.dl:hover { background: #1b5e20; }

.empty { color: #aaa; font-style: italic; padding: 18px 0; font-size: 14px; }

/* THUMBNAIL */
.thumb {
  width: 90px; height: 56px; object-fit: cover; border-radius: 6px;
  background: #f0f0f0; border: 1px solid var(--border-gray); flex-shrink: 0;
}

/* INFO TABLE */
.info-card .section-header {
  background: linear-gradient(180deg, var(--nissan-red) 0%, var(--nissan-red-dark) 100%);
}
.info-table { width: 100%; border-collapse: collapse; }
.info-table tr { border-bottom: 1px solid var(--border-gray); }
.info-table tr:last-child { border-bottom: none; }
.info-label {
  padding: 14px 18px 14px 0; font-weight: 700; color: var(--text-muted);
  font-size: 12px; text-transform: uppercase; letter-spacing: 0.8px; width: 190px; vertical-align: top;
}
.info-value { padding: 14px 0; color: var(--nissan-black); font-size: 16px; font-weight: 500; }
.code-pill {
  display: inline-block; background: #f0f0f0; padding: 4px 12px; border-radius: 6px;
  font-family: 'SF Mono', 'Courier New', monospace; font-weight: 700; letter-spacing: 1px; color: var(--nissan-black);
}

/* BATCH DIVIDER */
.batch-divider {
  background: var(--nissan-black); color: #fff; padding: 12px 20px; border-radius: 8px;
  font-weight: 700; letter-spacing: 1px; margin: 30px 0 16px; font-size: 14px; text-transform: uppercase;
}

/* COLLAPSIBLE */
.section-header.clickable { cursor: pointer; user-select: none; transition: filter 0.2s; }
.section-header.clickable:hover { filter: brightness(1.15); }
.toggle-arrow { display: inline-block; transition: transform 0.25s ease; font-size: 11px; margin-right: 4px; }
.section.collapsed .section-body { display: none; }
.section.collapsed .toggle-arrow { transform: rotate(-90deg); }

/* FOOTER */
.footer { text-align: center; padding: 36px 20px; color: #999; font-size: 12px; letter-spacing: 1px; }

/* RESPONSIVE */
@media (max-width: 640px) {
  .header { padding: 16px 20px; }
  .header-title { font-size: 11px; letter-spacing: 2px; padding-left: 14px; }
  .logo { height: 36px; }
  .hero h1 { font-size: 27px; }
  .hero p { font-size: 14px; }
  .card { padding: 24px; }
  .img-item { flex-direction: column; align-items: stretch; }
  .btn-group { justify-content: flex-end; }
  .info-label { width: auto; display: block; padding-bottom: 4px; }
  .info-value { display: block; padding-top: 0; }
  .thumb { width: 100%; height: 140px; }
}
